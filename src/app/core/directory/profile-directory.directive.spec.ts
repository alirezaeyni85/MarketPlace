import { ProfileDirectoryDirective } from './profile-directory.directive';

describe('ProfileDirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileDirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
